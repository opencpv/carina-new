import routes from "./routes";

const homePageData = () => {
  return {
    mainHeading: "Fastest and \n Transparent way to claim \n insurance",
    ctaLink: routes.gettingStarted,
  };
};

export default homePageData;
